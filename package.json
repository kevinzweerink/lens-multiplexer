{
  "name": "lens-multiplexer",
  "version": "0.0.0",
  "description": "A type of visualization that can be used to identify lines of flight between philosophical lenses",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-minify": "0.0.5"
  }
}
